<?php
/**
 * File autogenerated by WP_CLI::wp_maintenance.php
 * Date: {{date}}
 */
if (!defined('ABSPATH')){
	exit();
}

/**
 * Duration to stay in maintenance.
 */
$duration = {{duration}};

/**
 * Stay in maintenance only for the given period.
 * Then bail out.
 */
if ({{activation}} < time()){
    /**
     * Duration to stay in maintenance: {{userduration}}
     * Schedulled for activation on: {{activationDate}}
     */
    $upgrading = {{duration}};

    /**
     * Disable updates when future schedulling is defined.
     */
    if (!defined('DISABLE_FILE_MODES')){
    	define('DISABLE_FILE_MOD', true);
    }
}
?>